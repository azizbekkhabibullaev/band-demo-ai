-- Sentinel migration. Real schema migrations follow in 0001+.
-- This file exists so the migration runner has at least one file to apply
-- and we can verify the tracking infrastructure works.
SELECT 1;
